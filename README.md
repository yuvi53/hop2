#Intorducting Hop2 a rust implementation [autojump](https://github.com/wting/autojump)
well this thing is work in progress some features work other don't, if it sounds something of your use feel, free to use!

#How to make it work
1. clone the directory!
2. run command `cargo run --release`
3. place this snippit in your terminal `path/to/your/cloned/repo/hop.zsh/`(again, you should always inpect scripts before doing stuff like this!)
